{
  "name": "caveman-intro",
  "private": true,
  "version": "1.0.0",
  "description": "Club to Throttle — a 3-second hand-coded canvas cold open, rendered frame-exact to MP4/WebM/GIF.",
  "type": "module",
  "scripts": {
    "render": "node render.mjs --contact"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.2.0",
    "playwright": "^1.56.0"
  }
}
